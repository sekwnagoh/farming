<%= simple_form_for @product_option do |f| %>
	<%= f.hidden_field :product_id, value: @product_option.product_id %>
	<%= f.input :grade, collection: ProductOption.grades.keys %>
	<%= f.input :weight, collection: ProductOption.weights.keys %>
	<%= f.input :price %>

	<%= f.submit %>
<% end %>	